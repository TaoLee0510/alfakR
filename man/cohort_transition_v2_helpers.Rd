% Generated by roxygen2: do not edit by hand
\name{cohort_transition_v2_helpers}
\alias{filter_cohort_transition_records}
\alias{aggregate_cohort_transition_records_by_patient}
\alias{classify_cohort_transition_groups}
\alias{compute_transition_group_heterogeneity}
\alias{compute_transition_group_class}
\alias{compute_class_specific_borrowing}
\alias{estimate_patient_transition_shift}
\alias{apply_cohort_transition_overlay}
\alias{should_apply_cohort_transition_to_node}
\title{Cohort-transition v2 helper functions}
\usage{
filter_cohort_transition_records(
  records,
  cohort_transition_use_prior_dominated_records = FALSE,
  cohort_transition_use_boundary_records = FALSE,
  cohort_transition_max_delta_se = NULL,
  cohort_transition_max_delta_se_quantile = 0.75,
  cohort_transition_min_path_responsibility = 0.05,
  cohort_transition_min_observed_count = 1L,
  cohort_transition_zero_min_expected_count = 3,
  cohort_transition_zero_as_censoring_only = TRUE,
  ...
)

aggregate_cohort_transition_records_by_patient(
  records,
  grouping = c("gain_loss", "gain_loss_chr", "gain_loss_chr_burden", "exact_event"),
  cohort_transition_sd_floor = 0.05,
  cohort_transition_zero_weight_cap_ratio = 0.25,
  ...
)

classify_cohort_transition_groups(
  patient_group_summaries,
  cohort_transition_sd_floor = 0.05,
  cohort_transition_patient_sd_floor = 0.1,
  cohort_transition_min_patients_consistent = 3L,
  cohort_transition_min_effective_patients = 3,
  cohort_transition_min_effective_observed = 3,
  cohort_transition_effect_threshold = 0.02,
  cohort_transition_sign_consistency_threshold = 0.75,
  cohort_transition_high_heterogeneity_i2 = 0.5,
  cohort_transition_high_between_patient_sd = 0.1,
  cohort_transition_context_heterogeneity_drop = 0.25,
  ...
)

compute_transition_group_heterogeneity(
  patient_group_summaries,
  group_name,
  group_level,
  group_col,
  cohort_transition_sd_floor = 0.05,
  cohort_transition_effect_threshold = 0.02,
  ...
)

compute_transition_group_class(
  k,
  eff_k,
  effective_observed,
  mu,
  se_mu,
  tau,
  i2,
  sign_consistency,
  cohort_transition_min_patients_consistent = 3L,
  cohort_transition_min_effective_patients = 3,
  cohort_transition_min_effective_observed = 3,
  cohort_transition_effect_threshold = 0.02,
  cohort_transition_sign_consistency_threshold = 0.75,
  cohort_transition_high_heterogeneity_i2 = 0.5,
  cohort_transition_high_between_patient_sd = 0.1
)

compute_class_specific_borrowing(
  effect_class,
  cohort_transition_lambda_consistent_deleterious = 0.5,
  cohort_transition_lambda_consistent_neutral = 0.25,
  cohort_transition_lambda_consistent_beneficial = 0.15,
  cohort_transition_lambda_context_dependent = 0.3,
  cohort_transition_lambda_high_variable = 0,
  cohort_transition_lambda_sparse_unknown = 0,
  cohort_transition_lambda_global_fallback = 0.05,
  cohort_transition_sd_multiplier_consistent_deleterious = 1,
  cohort_transition_sd_multiplier_consistent_neutral = 1.5,
  cohort_transition_sd_multiplier_consistent_beneficial = 2,
  cohort_transition_sd_multiplier_context_dependent = 1.5,
  cohort_transition_sd_multiplier_high_variable = 4,
  cohort_transition_sd_multiplier_sparse_unknown = 4,
  cohort_transition_sd_multiplier_global_fallback = 4
)

estimate_patient_transition_shift(
  patient_records,
  prior,
  cohort_transition_patient_shift_min_records = 3L,
  cohort_transition_patient_shift_shrinkage_sd = 0.1,
  ...
)

apply_cohort_transition_overlay(
  item,
  child_name,
  build_opt_fc,
  search_interval,
  prior_use,
  f_two_shell_baseline,
  nn_present = NULL,
  two_shell_node_diagnostics = NULL,
  cohort_transition_apply_to = c("zero_only", "low_information", "all"),
  cohort_transition_lambda = 0.25,
  cohort_transition_max_borrowing_fraction = 0.5,
  cohort_transition_max_abs_delta_shift = NULL,
  cohort_transition_sd_floor = 0.05,
  cohort_transition_patient_sd_floor = 0.1
)

should_apply_cohort_transition_to_node(
  item,
  child_name,
  nn_present = NULL,
  two_shell_node_diagnostics = NULL,
  cohort_transition_apply_to = c("zero_only", "low_information", "all")
)
}
\arguments{
\item{records}{Raw or filtered transition records from \code{extract_cohort_transition_records()}.}
\item{patient_group_summaries}{Patient-level summaries from \code{aggregate_cohort_transition_records_by_patient()}.}
\item{patient_records}{Records or patient-level summaries for one patient.}
\item{prior, prior_use}{A cohort-transition prior object or patient-specific prior view.}
\item{item}{One nearest-neighbour child context from the ALFA-K bootstrap fitter.}
\item{child_name}{Nearest-neighbour child karyotype name.}
\item{build_opt_fc}{Function that builds the direct patient likelihood objective for a child context.}
\item{search_interval}{Numeric interval used for one-dimensional NN optimization.}
\item{f_two_shell_baseline}{Patient-specific two-shell baseline fitness for the child.}
\item{nn_present}{Optional logical indicating whether the child was observed in the current bootstrap replicate.}
\item{two_shell_node_diagnostics}{Optional one-row two-shell node diagnostics data frame.}
\item{grouping}{Transition grouping mode.}
\item{group_name, group_level, group_col}{Group label, group level, and column used for heterogeneity calculations.}
\item{k, eff_k, effective_observed, mu, se_mu, tau, i2, sign_consistency}{Summary statistics used by \code{compute_transition_group_class()}.}
\item{effect_class}{Transition group class such as \code{"consistent_deleterious"}, \code{"high_variable"}, or \code{"sparse_unknown"}.}
\item{cohort_transition_use_prior_dominated_records, cohort_transition_use_boundary_records}{Whether prior-dominated or boundary records may be used as observed transition labels.}
\item{cohort_transition_max_delta_se, cohort_transition_max_delta_se_quantile}{Absolute or quantile-derived delta-SE screen for observed transition labels.}
\item{cohort_transition_min_path_responsibility}{Minimum path responsibility for a raw path record.}
\item{cohort_transition_min_observed_count}{Minimum observed child count for observed delta labels.}
\item{cohort_transition_zero_min_expected_count}{Minimum parent-like expected count for zero-censoring records.}
\item{cohort_transition_zero_as_censoring_only}{Whether zero records are kept only as censoring evidence and never as observed delta labels.}
\item{cohort_transition_sd_floor, cohort_transition_patient_sd_floor}{Prior SD and patient heterogeneity floors.}
\item{cohort_transition_zero_weight_cap_ratio}{Cap on zero-censoring summary weight.}
\item{cohort_transition_min_patients_consistent, cohort_transition_min_effective_patients, cohort_transition_min_effective_observed}{Patient-level evidence thresholds for consistent group classes.}
\item{cohort_transition_effect_threshold, cohort_transition_sign_consistency_threshold, cohort_transition_high_heterogeneity_i2, cohort_transition_high_between_patient_sd, cohort_transition_context_heterogeneity_drop}{Group classification controls.}
\item{cohort_transition_lambda_consistent_deleterious, cohort_transition_lambda_consistent_neutral, cohort_transition_lambda_consistent_beneficial, cohort_transition_lambda_context_dependent, cohort_transition_lambda_high_variable, cohort_transition_lambda_sparse_unknown, cohort_transition_lambda_global_fallback}{Class-specific borrowing strengths.}
\item{cohort_transition_sd_multiplier_consistent_deleterious, cohort_transition_sd_multiplier_consistent_neutral, cohort_transition_sd_multiplier_consistent_beneficial, cohort_transition_sd_multiplier_context_dependent, cohort_transition_sd_multiplier_high_variable, cohort_transition_sd_multiplier_sparse_unknown, cohort_transition_sd_multiplier_global_fallback}{Class-specific prior SD multipliers.}
\item{cohort_transition_patient_shift_min_records, cohort_transition_patient_shift_shrinkage_sd}{Patient-specific transition-shift controls.}
\item{cohort_transition_apply_to}{Which NN nodes can receive the v2 overlay.}
\item{cohort_transition_lambda}{Global multiplier applied to class-specific borrowing strengths.}
\item{cohort_transition_max_borrowing_fraction}{Maximum allowed cohort borrowing fraction before an overlay is skipped.}
\item{cohort_transition_max_abs_delta_shift}{Optional maximum absolute fitness shift from the two-shell baseline.}
\item{...}{Additional controls passed through the v2 helper stack.}
}
\value{
Filtering and aggregation helpers return data frames or structured lists with
diagnostics. Classification helpers return transition group class tables or
class-specific borrowing controls. Overlay helpers return final fitness values
and per-path diagnostics.
}
\description{
Reusable lower-level helpers for \code{cohort_transition_v2}. They implement
record filtering, patient-level aggregation, group heterogeneity classification,
patient-specific shift estimation, and the guarded cohort-transition overlay on
top of patient-specific two-shell estimates.
}
\details{
These helpers keep raw patient counts unpooled and operate on transition effects
\code{Delta = child fitness - parent fitness}. Zero nearest neighbours are
treated as censoring evidence and are not converted into observed delta labels.
}
