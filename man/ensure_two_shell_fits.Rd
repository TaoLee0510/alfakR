% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cohort-transition.R
\name{ensure_two_shell_fits}
\alias{ensure_two_shell_fits}
\title{Ensure two-shell fits exist, reusing valid cached fits}
\usage{
ensure_two_shell_fits(
  patients,
  patient_ids = names(patients),
  two_shell_root = NULL,
  outdir,
  pm,
  minobs,
  ...,
  sample_names = NULL,
  pm_tag = NULL,
  minobs_tag = NULL,
  sample_map = NULL,
  reuse_two_shell = TRUE,
  rerun_missing_two_shell = TRUE,
  rerun_corrupt_two_shell = TRUE,
  integrity_check = c("strict", "basic", "none"),
  base_nn_prior = "empirical_two_shell",
  allow_incomplete_cohort = FALSE
)
}
\arguments{
\item{patients}{Named list of patient inputs accepted by \code{alfak()}.}

\item{patient_ids}{Patient identifiers aligned with \code{patients}.}

\item{two_shell_root}{Cache root. If \code{NULL}, \code{outdir/two_shell_base} is used.}

\item{outdir}{Cohort output directory where status tables are saved.}

\item{pm}{Mis-segregation probability for the upstream two-shell fit.}

\item{minobs}{MINIOBS threshold for the upstream two-shell fit.}

\item{...}{Additional arguments passed to \code{alfak()} when a sample must be rerun.}

\item{sample_names}{Optional sample directory names aligned with \code{patient_ids}.}

\item{pm_tag}{Optional exact PM cache tag.}

\item{minobs_tag}{Optional exact MINIOBS cache tag.}

\item{sample_map}{Optional named patient-to-sample directory map.}

\item{reuse_two_shell}{Reuse valid cached fits.}

\item{rerun_missing_two_shell}{Rerun only missing fits.}

\item{rerun_corrupt_two_shell}{Back up and rerun only corrupt fits.}

\item{integrity_check}{Integrity mode used for existing and rerun fits.}

\item{base_nn_prior}{NN prior mode used for rerun base fits.}

\item{allow_incomplete_cohort}{If \code{TRUE}, record rerun failures instead of
stopping immediately.}
}
\value{
A data frame with before/after status and actions.
}
\description{
Ensure two-shell fits exist, reusing valid cached fits
}
